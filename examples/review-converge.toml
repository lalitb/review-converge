# Configuration is loaded only when passed with --config.
reviewers = ["claude:opus", "codex:gpt-5.6-sol"]
final_decider = "codex:gpt-5.6-sol"
codex_reasoning_effort = "low"
rounds = 3
timeout = 1800
context_files = ["CONTRIBUTING.md"]
instructions = ["Review as a maintainer; prioritize behavioral and compatibility risks."]
fail_on = "request_changes"

# Optional per-invocation provider limits:
# claude_max_budget_usd = 2.00
# copilot_max_ai_credits = 30
# structured_retries = 1
